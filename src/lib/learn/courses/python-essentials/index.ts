// Canonical course entry point. The legacy implementation path is retained so
// existing authoring scripts can migrate independently from the public rename.
export { getAllModules } from "../compsci101";
